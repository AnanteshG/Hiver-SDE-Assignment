# Copy to .env. Hosted endpoints generally require a key.
# Use a provider that supports /chat/completions and JSON-object responses.
LLM_BASE_URL=
LLM_MODEL=
LLM_API_KEY=
# Optional independent judge model at the same endpoint; otherwise LLM_MODEL.
JUDGE_MODEL=
