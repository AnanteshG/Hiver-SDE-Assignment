# Decision log

1. Use Python for a single inspectable data, agent, and evaluation pipeline.
2. Keep the dashboard optional so evaluation runs without a web interface.
3. Begin with a dependency-free scaffold; add and pin runtime dependencies as their features are implemented and verified.
4. Ignore raw data and credentials from the first commit to avoid accidental publication.
5. Implement on a feature branch and commit verified milestones separately.
